{
  "name": "a-movies",
  "version": "0.0.0",
  "proxy": "http://localhost:5000",
  "scripts": {
    "con": "concurrently --kill-others \"npm run dev\" \"npm run backend\"",
    "dev": "vite",
    "backend": "cd backend && venv/bin/flask run --no-debugger",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "vue": "^3.2.37",
    "vue-router": "^4.1.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.1",
    "vite": "^3.0.1"
  }
}
